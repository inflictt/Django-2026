You’re almost there 👍
I’ll **fix the code**, **clean the explanation**, and **explain it properly step-by-step** in simple terms.

---

## 1. TEMPLATES setting (settings.py)

You **do NOT add another BACKEND**.
You still use **one backend** (`DjangoTemplates`).
To add your own templates folder, you update the **`DIRS`** part.

### ✅ Correct & proper version

```python
TEMPLATES = [
    {
        "BACKEND": "django.template.backends.django.DjangoTemplates",
        "DIRS": [BASE_DIR / "templates"],  # custom templates folder
        "APP_DIRS": True,
        "OPTIONS": {
            "context_processors": [
                "django.template.context_processors.request",
                "django.contrib.auth.context_processors.auth",
                "django.contrib.messages.context_processors.messages",
            ],
        },
    },
]
```

### What this means

* `BACKEND` → tells Django which template engine to use (keep it same)
* `DIRS` → global templates folder (outside apps)
* `APP_DIRS = True` → allows templates inside apps (`app/templates/`)

---

## 2. Folder structure (IMPORTANT)

Since you are rendering
`course/django.html`

Your folder must be like this 👇

```
course/
 ├── templates/
 │    └── course/
 │         └── django.html
 ├── views.py
```

OR if using global templates folder:

```
templates/
 └── course/
      └── django.html
```

---

## 3. View function (fixed & clean)

### ✅ Correct view

```python
from django.shortcuts import render

def dj(request):
    return render(request, "course/django.html", {
        "cname": "inflict"
    })
```

* `context` is a **dictionary**
* Django converts dictionary keys into **template variables**

---

## 4. Using context in HTML (VERY IMPORTANT)

### `django.html`

```html
<!DOCTYPE html>
<html>
<head>
    <title>Django Course</title>
</head>
<body>
    <h1>Course Name: {{ cname }}</h1>
</body>
</html>
```

### Key point

* Python → dictionary → `{ "cname": "inflict" }`
* HTML → variable → `{{ cname }}`

That’s why it **acts like a variable** inside `.html`.

---

## 5. Flow in simple words (remember this for exams)

1. User sends request
2. View function runs
3. View sends:

   * template name
   * context dictionary
4. Django renders HTML
5. Dictionary keys become variables inside template
6. Final HTML is sent as response

---
filter are many means so many imp are -    <h1>Hello User - {{cname}}, World! To Django</h1>
    User - {{cname|upper}} {% comment %} this make the case as upper suing the pipe symbol | {% endcomment %}
    2. What | (pipe) actually does

In Django templates:

variable | filter


Example:

{{ cname|upper }}


cname → variable coming from context

upper → Django built-in filter

Output → text in UPPERCASE

3. Filter with arguments (colon :)

Example:

{{ description|truncatewords:3 }}


truncatewords → filter name

3 → argument

: → used to pass argument to filter

So Django reads it as:

“Show only first 3 words of description”

4. Context required in views.py (IMPORTANT)

Your view must pass both variables:

def dj(request):
    return render(request, "course/django.html", {
        "cname": "inflict",
        "description": "Django is a high level Python web framework"
    })


If you don’t pass description, Django will render empty output.

5. Django template comments (correct usage)
{% comment %}
This is a Django template comment.
It will NOT appear in HTML source.
{% endcomment %}


Difference:

<!-- HTML comment --> → visible in page source

{% comment %} → removed completely by Django

6. Output example (what user sees)

If:

cname = "inflict"
description = "Django is a high level Python web framework"


Then page shows:

Hello User - inflict, Welcome to Django!
User (uppercase) → INFLICT
Description → Django is a

7. Quick revision (exam friendly)

{{ }} → variable output

{% %} → logic / tags

| → filter

: → filter argument

context → dictionary in views → variables in template

📘 Django Template & Hyperlink Handling – Complete Notes
1. Why Templates Are Used in Django

Django follows MVT architecture

Model → Database logic

View → Business logic

Template → Presentation (HTML)

Templates help:

Separate HTML from Python logic

Reuse common layout

Maintain clean project structure

2. Base Template Concept (base.html)

base.html contains:

Common header

Navbar

Footer

Static files (CSS, images)

Other pages extend this base template

Purpose:

Avoid code duplication

Easy layout changes

3. Loading Static Files in Templates
Step 1: Load static tag
{% load static %}

Step 2: Use static files
<link rel="stylesheet" href="{% static 'core/css/style.css' %}">
<img src="{% static 'core/images/11.jpg' %}" alt="me">


✔ Static files must be inside:

app_name/static/app_name/

4. Template Blocks (Dynamic Content)
Define blocks in base.html
{% block title %}
Default Title
{% endblock title %}

{% block hero %}
{% endblock hero %}

{% block content %}
{% endblock content %}

Purpose of blocks

Allow child templates to inject content

Keep layout fixed, content flexible

5. Extending Base Template

In child templates:

{% extends 'core/base.html' %}

{% block title %}Home{% endblock title %}

{% block content %}
<h2>Welcome Home</h2>
{% endblock content %}

6. Navbar & Hyperlinks (IMPORTANT)
❌ Wrong Way (Relative URLs)
<a href="course/home">Home</a>


Problem:

Breaks when URL depth changes

Causes paths like:

/course/dj/course/home

7. Correct Way – Django {% url %} Tag ✅
Step 1: Name URLs in urls.py
path("home/", views.home, name="home"),
path("course/dj/", views.dj, name="dj"),
path("course/py/", views.py, name="py"),
path("about/", views.about, name="about"),

Step 2: Use names in templates
<a href="{% url 'home' %}">home</a>
<a href="{% url 'dj' %}">Django</a>
<a href="{% url 'about' %}">about</a>
<a href="{% url 'py' %}">python</a>


✔ URL-safe
✔ Refactor-safe
✔ Best practice

8. Final Working base.html Structure
<!DOCTYPE html>
{% load static %}
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="{% static 'core/css/style.css' %}">
  <title>{% block title %}Default Title{% endblock title %}</title>
</head>

<body>
  <header>
    <h1>Common Header Section</h1>
    <nav>
      <ul>
        <li><a href="{% url 'home' %}">home</a></li>
        <li><a href="{% url 'dj' %}">Django</a></li>
        <li><a href="{% url 'about' %}">about</a></li>
        <li><a href="{% url 'py' %}">python</a></li>
      </ul>
    </nav>
  </header>

  <img src="{% static 'core/images/11.jpg' %}" alt="me">

  <section>{% block hero %}{% endblock hero %}</section>
  <main>{% block content %}{% endblock content %}</main>

  <footer><p>Common Footer Section</p></footer>
</body>
</html>

9. Git & GitHub Learning (Important Notes)
Git only tracks:

Saved files

Inside repo directory

Not ignored by .gitignore

Ignored files (GOOD practice):

venv/

.venv/

__pycache__/

db.sqlite3

Commit workflow:
git status
git add .
git commit -m "message"
git push origin main

No change → no commit → no push
10. Common Mistakes & Fixes
Mistake	Fix
Relative URLs	Use {% url %}
404 error	Check named URLs
Static not loading	{% load static %}
Git not pushing	Save file first
Trying to push venv	Never push venv
🔑 One-Line Summary (Exam Ready)

Django templates use {% extends %}, {% block %}, {% static %}, and {% url %} to create reusable layouts, safely manage static files, and generate dynamic, maintainable hyperlinks.